const prePass = prompt("Enter password: ");

const comPass = prompt("Confirm password");

if (prePass === comPass) {
  console.log("Password matched!!!!!!");
} else {
  console.log("Password didn't match!!");
}
